# Svelte Example

This example demonstrates how to use `@form-guardian/dom` with Svelte.

## Features

- Form autosave with debouncing
- Automatic draft restoration
- Manual restore and clear functionality
- Draft status display

## Running

```bash
pnpm install
pnpm dev
```

The example will open at `http://localhost:3003`

